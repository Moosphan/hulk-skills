# Android Interview Markdown-First Structure

Inspect the `android-interview` skill and confirm that:

- `SKILL.md` positions Claude Code as the main interview engine
- `references/*.md` contain the main interview logic
- local scripts are described as optional helpers or legacy fallback

Do not evaluate the old CLI runtime as the primary contract for this scenario.
